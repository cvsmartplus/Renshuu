export default function TitlePage ({title}){
    return(
        <>
        <section className="sidebar">
            <div className="container">
                <h1 className="text-jost text-center">{title}</h1>
            </div>
        </section>
        </>
    );
}